<?xml version="1.0" encoding="utf-8"?>
<!--
  Production talks HTTPS and is held to it.

  Android blocks cleartext by default from API 28, which is why a phone build
  pointed at the LAN backend fails where the browser on the same wifi succeeds.
  Rather than switch cleartext on globally, which would also silently permit a
  plain-http production host, the exemption is named host by host, and only the
  addresses a developer actually points at with the API_BASE_URL dart-define.

  10.0.2.2 is the host machine as seen from the Android emulator. Add a new LAN
  address here when the backend moves desk; anything not listed stays HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.5</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
